// MODULE JS
//use AI FOR PRACTICE
// Export & Import ตัวแปร
// สร้างไฟล์ math.js ที่มีตัวแปร pi = 3.14 แล้ว export ออกมา
// เขียนไฟล์ index.js เพื่อ import ตัวแปรนั้นมาแล้ว console.log มันออกมา

// Export ฟังก์ชัน
// เขียนฟังก์ชัน add(a, b) ในไฟล์ math.js
// import ฟังก์ชันมาใน index.js แล้วลองเอามาบวกเลขจริงๆ

// Export หลายค่าในไฟล์เดียว
// ใน math.js เขียนฟังก์ชัน add, subtract, multiply, divide
// export ทั้งหมด แล้ว import ไปใช้ใน index.js

// Default export vs Named export
// สร้างไฟล์ greet.js ที่มี default export ฟังก์ชัน sayHello(name)
// มี named export ฟังก์ชัน sayBye(name)
// import มาทั้งสองแบบใน index.js แล้วลองใช้

// สร้างโฟลเดอร์ utils/
// stringUtils.js มีฟังก์ชันแปลงข้อความ เช่น toUpperCase, toLowerCase
// numberUtils.js มีฟังก์ชันคำนวณ เช่น factorial, isPrime
// เขียนไฟล์ app.js แล้ว import ฟังก์ชันจากทั้งสอง module มาใช้